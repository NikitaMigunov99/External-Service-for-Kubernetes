apiVersion: v1
kind: ConfigMap
metadata:
  name: config-external-service-configmap
  namespace: dev
data:
  application.yaml: |-
    spring:
      application:
        name: external-service

    server:
      port: 8090

    myapp:
      custom:
        value: My favorite value